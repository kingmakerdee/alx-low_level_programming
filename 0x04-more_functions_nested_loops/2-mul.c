#include "main.h"

/**
 * int mul - multiplies 2 numbers
 * @a: first num to be multiplied with b
 * @b: second num to be multiplied with a
 * Return: returns result
 */

int mul(int a, int b)

{
	return (a * b);
}
